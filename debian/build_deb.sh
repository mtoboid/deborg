#!/usr/bin/bash

set -o errexit
set -o pipefail
set -o nounset
# script to build a debian package for deborg

# 1) ensure we have a venv
# TODO

# 2) activate the python venv and build the python package
# TODO

# 3) build the debian package

main() {
    echo "main"
}

main "$@"
